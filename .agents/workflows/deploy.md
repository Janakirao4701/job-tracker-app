---
description: How to deploy the Job Tracker application
---

// turbo-all
1. Run the deployment script from the root directory:
   ```powershell
   .\deploy.ps1
   ```
2. Verify that the git commands succeeded in the terminal output.
